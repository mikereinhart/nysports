%body
  / Header and Nav
  / End Header and Nav
  .row
    / Nav Sidebar
    / This is source ordered to be pulled to the left on larger screens
    
    / Main Feed
    / This has been source ordered to come first in the markup (and on small devices) but to be to the right of the nav on larger screens
    .large-8.columns
    / Feed Entry
    .row
      .large-10.columns.brand-text
        %h3.dark.brand-text Hey #{@user.name.capitalize}! Find out what's new:
        %h5.white.sans Get started by using the nav bar to select what team you want to read about
      %ul.sports_feed
        - for entry in Feed.all(:limit => 80, :order => "published_at desc")
          %img{:src => entry.thumbnail}/
          = link_to h(entry.name), entry.feed_url
          = raw truncate(strip_tags(entry.summary), :length => 300)
          %br/
          %hr/
  / End Feed Entry
