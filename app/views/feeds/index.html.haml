%h1 Index Feeds Page
%h3
%ul
  - @feeds.each do |feed|
    %li
      %p= link_to "#{feed['name']}", feed_path(feed)
      /= link_to 'Edit Feed', edit_feed_path(feed)
      /= link_to "Delete Feed", feed_path(feed), method: :delete
      %br/
      %br/
